package org.top.java.source.collection;

import java.util.*;

public class Test {
    public static void main(String[] args) {
        Iterable  iterable;
        Collection collection = null;
        List list1 = null;
        Set set = null;

        Map map = null;
        Queue queue = null;
        Object object;

        ArrayList arrayList;
        LinkedList list;
        HashSet hashSet1;
        HashMap hashMap;
        TreeSet treeSet;
        TreeMap treeMap;
        LinkedHashSet linkedHashSet;
        LinkedHashMap linkedHashMap;
        PriorityQueue priorityQueue;
        Vector       vector;
        Stack stack;
        Arrays arrays;
        Comparable comparable;

        String s;
    //     Collections.checkedCollection() ;
    // Collections.synchronizedCollectio
    }

    public static void view(){
        // List<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3, 4, 5));
        // List<Integer> subList = list.subList(1, 4);  // 返回原 List 的部分视图
        // System.out.println(list);
        // subList.set(0, 10);  // 修改视图集合，原 List 会受到影响
        // System.out.println(list);  // 输出：[1, 10, 3, 4, 5]
        TreeMap map;
    }
}
